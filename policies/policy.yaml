policies:
  - name: all-iam-role
    resource: aws.iam-role
    conditions:
      - region: us-east-1
  - name: all-lambda
    resource: aws.lambda
  - name: s3-set-bucket-encryption
    resource: s3
    actions:
      - type: set-bucket-encryption
        crypto: AES256
        enabled: True

